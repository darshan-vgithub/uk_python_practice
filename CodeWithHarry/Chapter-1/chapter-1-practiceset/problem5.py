# Label the program written in problem 4 with comments.


# Import the 'os' module from Python's standard library
# This module lets us interact with the operating system (like files, folders, paths).
import os  

# Use the os.listdir() function to list all files and directories 
# in the current working directory.
# The print() function then outputs that list to the console.
print(os.listdir())  
